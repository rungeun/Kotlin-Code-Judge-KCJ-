package com.github.rungeun.kcj.kotlincodejudge

import com.intellij.ui.JBColor
import java.awt.Color

object Colors {
    //val a: JBColor = JBColor(라이트 모드, 다크 모드)
    val outerBackground: JBColor = JBColor(Color.GREEN, Color.GREEN)
    val innerBackground: JBColor = JBColor(Color.WHITE, Color.WHITE)

    // 추가로 필요한 색상 정의
    val textColor: JBColor = JBColor(Color.BLACK, Color(0x212121))
    val borderColor: JBColor = JBColor(Color.GRAY, Color(0x424242))
}
